using System.Collections;
using System.Collections.Generic;
using System.Globalization;
using UnityEngine;
using UnityEngine.UI;
/*
public class RandomColor : MonoBehaviour
{
    [SerializeField] private float ChangeInterval = 1.0f;
    private float localChangeInterval;
    [SerializeField] private Image d_image;

    // Start is called before the first frame update
    void Start()
    {
        localChangeInterval = 0f;
        //d_image.color = new Color(Random.value, Random.value, Random.value);
    }

    // Update is called once per frame
    void Update()
    {
        localChangeInterval += Time.deltaTime;

        if (localChangeInterval < ChangeInterval) return;
        {

            d_image.color = new Color(Random.value, Random.value, Random.value);
            d_image.rectTransform.localScale = new Vector3(Random.value, Random.value, Random.value);

            localChangeInterval -= ChangeInterval;
        }
        //d_image.rectTransform.localScale = new Vector3(Random.value, Random.value, Random.value);
        //Random.Range(-100,100);

    }

}
*/